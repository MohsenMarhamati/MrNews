@{
    ViewData["Title"] = "ورود";
    Layout = "/Views/Shared/_LogLayout.cshtml";
    string url = @ViewBag.url;
}

<div class="Login-to-account mt-4">
    <div class="account-box-content">
        <h4>ورود به حساب کاربری</h4>
        <div class="form-account-title">
            <label for="email-phone">ایمیل  </label>
            <input type="text" class="number-email-input" id="Email" name="email-account" onkeydown="CheckKeyPress(this,event)">
        </div>
        <div class="form-account-title">
            <label for="password">رمز عبور</label>
            <input type="password" class="password-input" id="Password" onkeydown="CheckKeyPress(this,event)">
        </div>
        <div class="form-auth-row">
            <label for="#" class="ui-checkbox mt-1">
                <input type="checkbox" value="1" name="login" id="remember">
            </label>
        </div>
        <div class="form-row-account">
            <a onclick="Login()" style="color:white;" class="btn btn-primary btn-login">ورود به آقای خبر</a>
            <a href=@url.Replace("\"", "") class="btn btn-register btn-danger">بازگشت</a>
        </div>
    </div>
</div>

@section Scripts {
<script>
    $('.ds').removeClass("active")
    $('.login-ds').addClass("active")


    function Login() {
        var postData = {
            'Email': $("#Email").val(),
            'Password': $("#Password").val(),
        };
        var url='/Authentication/Signin';
        $.post(url,postData,function(data){
             if(data.isSuccess){
                 var u = (new URL(window.location.href)).searchParams.get("ReturnUrl");
                 window.location.href = u.replaceAll("\"", "");
             }
             else{
                 swal.fire('هشدار',data.message,'warning');
             }
        });
    }


    function CheckKeyPress(el, e) {
            if (e.keyCode == 13) {
                debugger
                switch( $(el).prop('id') ){
                    case "Email" : {
                        $("#Password").focus();
                        break;
                    }
                    case "Password" : {
                        Login();
                        break;
                    }
                }
            }
        }
</script>
}

